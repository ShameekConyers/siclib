# import pysiclib

# my_list = [1.0, 2.0, 4.0]
# tensor = pysiclib.linalg.Tensor(my_list)
# tensor2 = pysiclib.linalg.Tensor(my_list)
# def my_func(x, y):
# 	return x + y
# result = tensor.binary_operation(tensor2, my_func)

import sicexample

sicexample.anime.add(1, 2)
